# MedGemma-Navigator-DICOMweb-FHIR
A compact “Navigator” demo that links FHIR patient context with DICOMweb imaging and runs MedGemma to produce a single saved run artifact (FHIR + DICOM trace + model output).
